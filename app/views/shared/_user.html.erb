<div id="user">
  <% if user.present? %>
    <img id="user-img" src= <%= user.photo %> alt="user_image"/>
    <h2><%= user.name %></h2>
    <p id="post-count">Number of posts: <%= user.posts_counter %></p>
  <% else %>
    <p>User not found</p>
  <% end %>
</div>